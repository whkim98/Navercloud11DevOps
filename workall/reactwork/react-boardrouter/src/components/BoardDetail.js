import { Alert } from '@mui/material';
import React from 'react';

const BoardDetail = () => {
    return (
        <div>
            <Alert>상세보기</Alert>
        </div>
    );
};

export default BoardDetail;