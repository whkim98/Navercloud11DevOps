import { Alert } from '@mui/material';
import React from 'react';

const BoardList = () => {
    return (
        <div>
            <Alert>게시판목록</Alert>
        </div>
    );
};

export default BoardList;