import { Alert } from '@mui/material';
import React from 'react';

const BoardForm = () => {
    return (
        <div>
            <Alert>게시판 글쓰기</Alert>
        </div>
    );
};

export default BoardForm;