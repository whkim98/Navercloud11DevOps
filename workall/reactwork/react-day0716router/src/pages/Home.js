import { Alert } from '@mui/material';
import React from 'react';

const Home = () => {
    return (
        <div>
            <Alert severity='success'>Home</Alert>
        </div>
    );
};

export default Home;